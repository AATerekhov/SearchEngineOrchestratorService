namespace Orchestrator.Domain.Enums
{
    public enum IndexJobStatus
    {
        Pending,
        InProgress,
        Succeeded,
        Failed,
        PartiallySucceeded,
        Cancelled
    }
}
